# Waveform Annotation Guide (Informative)

- Use markers for `start`, `plugins_ok`, `true_state`, `executed`.
- Record the evaluated max-abs error and compare to epsilon in the same timescale.
- For each conformance vector, annotate where plugin status changes occur.
